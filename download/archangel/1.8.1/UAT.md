# Archangel 1.8.1 - physical check

This is a plan, not a record of executed native tests. Use a complete profile backup.

1. Quit the old public-line client. Start the complete 1.8.1 folder. Confirm the
   launcher, sidebar, backend and installed package report 1.8.1. Identity and
   accepted notes should remain unchanged. Do not reuse a live experimental profile.
2. Click Share summary with an old saved delta present. Expect blank fields at
   Write. Close with X, reopen and repeat with Escape. Neither action sends.
3. Resume the old draft only through Drafts. Confirm its text, mode and original
   delivery locks remain. New message starts blank; Discard affects only the draft.
4. Send a genuinely new harmless note to an approved rig, check remote acceptance
   and indexing, then ask a fresh receiving-side question and inspect its evidence.
   Never use a fresh message to bypass an uncertain receipt.
5. Quit/restart, inspect persistence, and test native window close/OS permissions.
   Attempt to start 1.8.1 while an older version runs: expect a clear stop message,
   not an older window presented as the new application.

Record exact versions and results per platform. This task did not execute these
steps on physical Mac/Windows rigs or with real local model weights.
